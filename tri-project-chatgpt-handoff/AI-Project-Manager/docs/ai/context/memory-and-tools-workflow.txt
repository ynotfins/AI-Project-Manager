Perform an audit to make sure the rules are forcing the use of our 7 primary tools to be used optimally. We don't have to have rules to use everything but clear thought, serena, context7, exa search, playwright, firecrawl, and magic should be used to maximum effect. But I have been disabling some of these tools because they eat up so much context when they are not used so we need to have a system that if the tool is not available the agent stops and notifies user because it just takes a second to click it back on. I don't love that as the system we have, develop a better workflow system and automate this if you can so i don't have to disable the tools to save context window, maybe there is a way for the agent to turn the tools off and b ack on when they are needed. You are Plan so you do not make edits to the files, is it possible for you to write a prompt to have Agent turn the tool you need on in one of the json files, if so this is our solution, write into the rules that Plan has to turn the major context eating tools off after every Agent execution, and then write into the rules to have Plan write a prompt to turn the tools back on that Plan needs. Or maybe create the rule for plan that after every Agent prompt is written we run that prompt through another tab with a Plan agent that takes all the build plans and runs them with the original prompt  using the tools, so the original Plan tab does not use the tools and then the second "Plan Tools tab" runs using the tools. I'm just brainstorming trying to think of a solution. Is there an extension that can help with this or even another mcp server. Any tool that helped reduce unnecessary context consumption and tool use would be well worth having. Can this be managed by disabling the ,json files that control any of these tools?

Remove Gitkraken mcp. remove shell mcp, remove sequential thinking unless it is needed for clear thought but i don't think it is. 

give me a prompt to give to debug to critique the new workflow and strategy. Explain to debug our goal of keeping context as low as possible but not so it hurts our design of a efficient functional system. Explain that we want to maximize the use of the 7 primary mcp servers clear thought, context7, serena, exa search, playwright, firecrawl, and magic. openmemory is extremely important because this is where we should be able to save all important ongoing info that we always have access to. We should create a system similar to Losslessclaw (https://github.com/martian-engineering/lossless-claw ) losslessclaw plugin has a DAG-based summarization system that preserves every message while keeping active context within model token limits. Create this memory system using openmemory mcp server or API you have the openmemory API Key in bitwarden and now you have Machine control, test this and confirm it is working properly, the Bitwarden Machine account I'm talking about.

This system should be for this entire PC and all projects. each project should be setup like it's own individual losslessclaw system like losslessclaw has for openclaw, but you manage all projects so this has to cover all projects and each project has to contain it's own ecosystem to protect from one project contaminating another. You have to be able to access each projects memories and have that project's unique data that only applies to it. There should also be a governance layer that you access that applies to everything and forces containment. Create a NO_LOSS.md file that describes everything you design because we are going to have Debug tab critique the new system with the mcp server rules and how they are integrated into this system. Use openmemory and the openmemory mcp server to have limitless access to store data. Keep an eye on the context window of Plan and Agent but mostly Plan because agent we should be starting a new chat pretty much every prompt and Agent is usually being used as non-thinking.

Look at the most recent 3 screenshots i took. That is a screenshot of every secret we have stored in bitwarden with the project it is in and the bitwarden UUID. The openmemory API key is CURSOR_LOSSLESS_OPENMEMORY_API_KEY and i created a new project in mem0 that is the mother company of the openmemory mcp server and system we use. The new project in mem0 is R3lentle$$-Grind-Ecosystem this project is going to be used for the global memory system we are building. Let me know if i have everything set up correctly on the mem0 website. I created a Machine account for you the Machine account name is R3lentle$$-Grind-Global-Memory

We have a md file that has all Bitwarden secret names, projects and UUID's on it, update to the current list i took a screenshot of. I also took 2 screenshots of the Machine accounts

Here are the environment variables for the bitwarden Machine Account:
Identity server URL
https://vault.bitwarden.com/identity

API server URL
https://vault.bitwarden.com/api

Organization ID
8098135b-9af5-41d7-9bcc-b3fa001d7cea

I added a local.env file at D:\github\AI-Project-Manager\docs\local.env with the token for the Machine account the name of the token is CURSOR_LOSSLESS_OPENMEMORY_API_KEY

I am not sure if i set this up correctly test this and make sure you can access this and you now have the ability to automate Bitwarden. If i need to add anything just tell me. Clean up our docs because we made a lot of changes and confirm that all docs are accurate. Add the new workflow in detail, we need to have a comprehensive detailed workflow that explains the rules for the tools and the new memory system         Cross reference all docs with each other. Any docs that remain confirm that you have cross referenced them with each other and all docs are accurate and align with our new workflow system. 

I added the BWS_ACCESS_TOKEN to the local.env file, make sure this file is on .gitignore
I gave you a screenshot of all the secrets saved for every project, the secret you need is in there. Do you want me to add it under the Machine account. I have never setup a machine account before so if i don't do it correctly just tell me exactly step by step what to do. And make sure we are  perfectly organized and save the secret names and UUID's and the projects that i sent you the screenshot of. There  are not secrets in the screenshots i sent you, the UUID looks like a secret but it is not, and the same is true with the environment variables i sent the screenshot of, there are no secrets in any of those screenshots. 

Update the build plan with the last two messages of info. Be sure to reread the original chat message that i sent three messages ago including this one as the first. 

Create a prompt to give debug to critique the new system.